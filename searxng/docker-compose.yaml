services:
  searxng:
    container_name: searxng
    image: searxng/searxng:latest
    pull_policy: always
    restart: unless-stopped
    ports:
      - 8080:8080
    volumes:
      - ./config:/etc/searxng:rw
