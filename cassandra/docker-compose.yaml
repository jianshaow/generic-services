version: "3"
services:
  cassandra:
    image: cassandra:${image_ver}
    container_name: cassandra
    restart: unless-stopped
    # environment:
    #   - 
    # volumes:
    #   - ${data_path}:/var/lib/cassandra
    ports:
      - 9042:9042
